import styled from "styled-components";

const instructionBox = styled.textarea`
  font-size: large;
`;
const titleBox = styled.input`
  font-size: x-large;
  text-align: center;
`;
const centeredDiv = styled.div`
  text-align: center;
  margin: 1em;
`;
const styles = {
  instructionBox,
  titleBox,
  centeredDiv,
};

export default styles;
